# 项目设计
---
# 私人内部向量数据库构建与可视化全流程规范文档

本探索规范旨在定义从零构建本地私有化向量数据库的完整工程生命周期。全流程共分为 **5 大核心阶段**，涵盖了从原始多模态私有数据到前端 WebGL 3D 降维可视化的完整数据流向。

---

## ── 流程总览 ──


```

[阶段一：数据准备与预处理] ──> [阶段二：向量化与 Embedding] ──> [阶段三：数据库选型与存储] ──> [阶段四：检索与算法优化] ──> [阶段五：前端可视化与应用]

```

---

## 阶段一：数据准备与预处理 (Data Ingestion & Preprocessing)
**职责**：将企业或个人内网中零散、异构的原始私有知识，转化为结构化、高纯净度的文本片段。

### 1.1 多源数据收集 (Multi-source Data Ingestion)
* **业务内涵**：全面扫描并导入本地或私有服务器中的非结构化数据。
* **数据来源**：企业网盘、员工手册、Markdown 知识库、项目 Wiki、Word/PDF 文档、研发 QA 历史对。
* **可视化埋点信息**：读取文件路径、文件大小、数据源类别标签。

### 1.2 文本清洗与标准化 (Text Cleaning)
* **业务内涵**：去除文本中的噪声。通过脚本自动过滤无意义的乱码、未闭合的 HTML 标签、多余的空白符及特殊控制字符，确保输入流的纯净度。
* **可视化埋点信息**：清洗前后的字符大小对比、清洗进度百分比。

### 1.3 智能重叠分块 (Smart Chunking)
* **业务内涵**：由于 Embedding 模型存在上下文窗口上限（Context Window），大文章必须切分。采用固定步长（如 500 字/块）进行切分，并设置 **10% ~ 20% 的重叠度（Overlap）**，防止由于物理切断导致上下文语境断裂。
* **可视化埋点信息**：单篇文章拆分后的 Chunk 总数、重叠区间高亮示意。

---

## 阶段二：向量化与模型嵌入 (Vector Embedding Generation)
**职责**：利用人工智能模型，将人类可读的文本“翻译”为计算机可计算的高维连续向量（数字坐标）。

### 2.1 离线 Embedding 模型加载 (Model Loading)
* **业务内涵**：在本地/内网 GPU 服务器上，通过 Rust `ort` (ONNX Runtime) 或 `candle` 框架本地加载开源轻量级向量模型（如 `bge-large-zh-v1.5`、`all-MiniLM-L6-v2`）。**数据绝不联网，从源头杜绝隐私泄露**。
* **可视化埋点信息**：显存占用率、模型加载状态、模型参数维度（如 768维 或 1536维）。

### 2.2 高维向量推断 (Vector Inference)
* **业务内涵**：将阶段一分好的 Chunks 批量输入模型，模型通过深度神经网络计算，输出该文本片段在语义空间中的绝对坐标——一个包含成百上千个浮点数的数组。
* **数据示例**：`"什么是公司报销流程？" -> [0.0123, -0.0456, 0.8912, ...]`
* **可视化埋点信息**：Token 吞吐速度、实时计算出的高维数组采样。

### 2.3 元数据动态绑定 (Metadata Mapping)
* **业务内涵**：向量本身是冰冷的数字，无法还原原文。此步骤将计算出的高维向量与原始文本、文件路径（如 `source: "/doc/薪酬方案.pdf"`）、创建时间等信息打包封装，以便检索时反查。
* **可视化埋点信息**：Payload 数据结构体 JSON 预览。

---

## 阶段三：向量数据库存储与索引 (Storage & Fast Indexing)
**职责**：将海量向量持久化到本地专用数据库中，并在高维空间中构建极其高效的“交通网络”（索引）。

### 3.1 数据库初始化与集合创建 (Collection Setup)
* **业务内涵**：在本地环境（推荐通过 Docker 部署）启动向量数据库（如 Qdrant 或 Milvus）。配置专用的集合（Collection），并严格指定**距离度量标准**（如余弦相似度 Cosine、点积 Dot Product 或 欧氏距离 L2）。
* **可视化埋点信息**：数据库连接状态（ping 值）、集合参数配置表。

### 3.2 批量高速写入 (Batch Upsert)
* **业务内涵**：前端 Next.js 触发、Tauri Rust 后端执行高并发操作，将 `[向量 + 元数据]` 批量安全写入底层存储引擎（如 RocksDB 或 LMDB）。
* **可视化埋点信息**：写入并发数、每秒写入条数（QPS）。

### 3.3 构建 HNSW 近似最近邻索引 (Index Building)
* **业务内涵**：这是毫秒级检索的核心。数据库在后台利用 **HNSW（分层可导航小世界网络）** 算法，将高维空间中的各个向量点相互连接，构建一个多层的导航图网，大幅降低暴力检索的计算量。
* **可视化埋点信息**：内存消耗曲线、HNSW 构图进度、当前总节点数。

---

## 阶段四：实时检索流与算法优化 (Query & Similarity Retrieval)
**职责**：当用户提问时，系统在毫秒级时间内从数百万条私有知识中找出最相关的上下文。

### 4.1 用户提问向量化 (Query Embedding)
* **业务内涵**：用户在 Next.js UI 中输入问题（如：“差旅费怎么报销？”），Next.js 将字符串通过 Tauri Bridge 传给 Rust。Rust 实时调用与阶段二**完全相同**的本地模型，将问题转化为查询向量（Query Vector）。
* **可视化埋点信息**：输入框拦截状态、查询向量生成耗时。

### 4.2 向量空间近似搜索 (ANN Search)
* **业务内涵**：拿着查询向量去数据库的 HNSW 索引图网中进行快速路由游走，寻找距离当前查询点最近的前 $K$ 个向量点（即最相似的本地知识）。
* **可视化埋点信息**：检索耗时（毫秒级）、检索到的前 K 个相似点的原始距离得分（Score）。

### 4.3 混合检索与重排优化 (Hybrid Search & Rerank)
* **业务内涵**：为了弥补向量对精确关键字不敏感的问题，引入传统传统关键词检索（BM25）与向量检索混合。随后调用本地轻量级重排模型（Reranker），对召回的前几十条数据进行二次精准打分排序，剔除低相关性噪声。
* **可视化埋点信息**：召回合并率、重排前后得分对比曲线。

---

## 阶段五：前端可视化与 RAG 交付 (WebGL Visual & RAG Application)
**职责**：将冰冷的后端数据变成极具未来感的 3D 视觉体验，并将清洗好的上下文交付给本地大模型。

### 5.1 高维空间降维处理 (Dimensionality Reduction)
* **业务内涵**：768 维空间的几何形态人类无法直观肉眼看到。Rust 后端利用 **t-SNE**、**UMAP** 或 **PCA** 数学降维算法，将所有向量点的坐标映射为三维空间坐标 `[x, y, z]`。
* **可视化埋点信息**：降维算法收敛迭代次数、`[x, y, z]` 坐标矩阵。

### 5.2 WebGL 3D 粒子星空渲染 (Three.js/R3F Rendering)
* **业务内涵**：Next.js 接收到降维后的坐标数组。通过 **React Three Fiber (R3F)** 驱动 GPU，在 WebGL 画布上将每一个 Chunk 绘制为星空中一颗发光的粒子。用户的查询向量显示为一颗高亮红星，并向最临近的 K 颗知识粒子发射光线连线。
* **可视化埋点信息**：FPS（帧率）、粒子群材质属性、视点相机矩阵。

### 5.3 液态玻璃面板流式交互与 RAG 交付 (Glass UI & LLM Prompt)
* **业务内涵**：检索出最相关的 Top-K 本地文本，在屏幕中央的**3D 动态液态玻璃面板**上以高逼真毛玻璃浮窗形式实时流式弹出（利用 `@react-three/drei` 物理折射材质）。同时，这些原文被组装为 Prompt 上下文，喂给内网本地大模型（如通过 Ollama 驱动的 Llama3），由大模型根据私有知识生成最终精准的答复。
* **可视化埋点信息**：大模型 Token 吐字速率（Tokens/s）、3D 液态玻璃折射率、流体动态位移变量。

---

# 项目目录结构设计
```
my-tauri-vector-app/
├── .vscode/                   # VS Code 项目级配置（如之前配置的 rust-analyzer 规则）
├── public/                    # 静态资源（全公开，不会被 Webpack 编译）
│   ├── icons/                 # 系统图标
│   └── models/                # 3D 预制模型文件（如 .gltf, .obj，如果有的话）
│
├── src/                       # NEXT.JS 前端核心源码
│   ├── app/                   # Next.js App Router 路由控制中心
│   │   ├── layout.tsx         # 全局布局（挂载 3D Canvas 的绝佳位置）
│   │   ├── page.tsx           # 主页（看板）
│   │   └── globals.css        # Tailwind CSS 全局样式
│   │
│   ├── components/            # 公用 UI 组件（2D）
│   │   └── ui/                # 按钮、输入框等原子组件（如 Shadcn UI 规范）
│   │
│   ├── features/              # 核心业务/视觉领域模块（重点）
│   │   ├── liquid-glass/      # ✨ 3D液态玻璃专属模块
│   │   │   ├── GlassPanel.tsx # 玻璃网格 React 组件
│   │   │   ├── glass.vertex.glsl   # 负责流体波动的顶点着色器
│   │   │   └── glass.fragment.glsl # 负责折射毛玻璃的片元着色器
│   │   │
│   │   └── vector-stars/      # ✨ 向量粒子星空模块
│   │       ├── StarField.tsx  # 粒子群组件
│   │       └── useVectorData.ts# 实时接收 Rust 向量数据的自定义 Hook
│   │
│   ├── services/              # 服务层（通信中枢）
│   │   └── tauriBridge.ts     # 封装所有 window.__TAURI__ 的 invoke 调用，隔离底层
│   │
│   └── hooks/                 # 全局复用状态逻辑
│
├── src-tauri/                 # RUST 后端核心源码
│   ├── src/
│   │   ├── commands/          # 存放各种 Tauri 命令模块
│   │   │   ├── vector_db.rs   # 负责连接和查询 Qdrant/Milvus 的 Rust 逻辑
│   │   │   └── mod.rs
│   │   └── main.rs            # Rust 程序入口，注册 command 和事件发射器
│   ├── Cargo.toml             # Rust 依赖配置文件
│   └── tauri.conf.json        # Tauri 桌面壳配置文件
│
├── next.config.mjs            # Next.js 配置文件 (已设置 output: 'export')
├── tailwind.config.ts         # Tailwind 样式配置文件
└── package.json               # 前端依赖管理
```
---
1. 着色器紧贴组件（features/liquid-glass/）：
把 .glsl 文件直接和引用它的 GlassPanel.tsx 放在一个文件夹下。开发 3D 特效时，你不需要在各种深层目录间切来切去，心智负担极低。

2. 前后端绝对隔离：
src/ 纯前端，src-tauri/ 纯后端。Rust 后端通过 commands/ 目录将数据库逻辑细分。如果以后你想做网页版，src-tauri/ 保持不动，只需修改前端 services/ 里的 API 适配即可。

3. Canvas 挂载于全局 Layout：
在 src/app/layout.tsx 中挂载 Three.js 的 `<Canvas>`，可以让 3D 场景作为全局背景存在。当你在 Next.js 中切换不同页面（路由）时，3D 背景不会重新销毁再创建，从而保证了液态玻璃流体波动的连续性，绝不闪烁。