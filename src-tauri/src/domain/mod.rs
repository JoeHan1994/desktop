//! Domain models — pure data structures with no I/O dependencies.
//!
//! Re-exports all domain types for convenient `crate::domain::*` imports.

pub mod model_provider;
pub mod pipeline;
pub mod remote_profile;
